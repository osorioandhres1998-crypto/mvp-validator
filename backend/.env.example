# Nivel de logging del backend.
LOG_LEVEL=INFO

# --- Integración con Claude (opcional) ---
# Si se define, el endpoint POST /ideas/analyze usará Claude para generar los
# arquetipos de audiencia y los insights. Si se omite, se usa una heurística
# determinista (sin red), por lo que el flujo funciona igualmente.
# ANTHROPIC_API_KEY=sk-ant-...
# LLM_MODEL=claude-sonnet-4-6
